name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Musical to Pages
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        
      - name: Check build output
        run: |
          echo "Checking if dist/_worker.js exists..."
          ls -lh dist/
          if [ ! -f "dist/_worker.js" ]; then
            echo "Error: dist/_worker.js not found!"
            exit 1
          fi
          echo "Checking for static files..."
          ls -lh dist/*.svg || echo "Warning: SVG files not found"
          echo "Build output verified!"
        
      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name musical
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
